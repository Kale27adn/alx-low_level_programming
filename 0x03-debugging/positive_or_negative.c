#include "main.h"

/**
 * main -Entry
 *
 * Return: Always 0 (sucess)
 */

int main(void)
{
	int i;
	{
	if (i > 0)
	{
		printf("%d is positive\n", i);
	}
	else if (i == 0)
	{
		printf("%d is zeron\n", i);
	}
	else
	{
		printf("%d is negative\n", i);
	}
}
return (0);
}
