#include<stdio.h>
#include<stdlib.h>

/**
 * Main - Entry
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	int  (ch = 'a');

	while (ch <= 'z')
	{
		putchar(ch "\n");
		ch++;
	}
	return (0);
}
